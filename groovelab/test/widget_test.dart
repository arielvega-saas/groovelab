// GrooveLab smoke test — verifies app can be instantiated.
// Real tests are in test/unit/ subdirectories.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('GrooveLab test suite placeholder', () {
    // Unit tests are in test/unit/
    // This file exists to satisfy Flutter's default test discovery.
    expect(true, isTrue);
  });
}
